<h1>商品一覧</h1><h3>全<%= @items.count %>件</h3>

<% @items.each do |item| %>
  
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.image, size: "250x150" %>
  <% end %>
  
  <%= item.name %>
  
  <p>¥<%= item.price %></p>
<% end %>